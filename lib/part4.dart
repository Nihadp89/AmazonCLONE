import 'package:flutter/material.dart';
import 'main.dart';

class y extends StatelessWidget {
  
  Widget build(BuildContext context) {
    
    return MaterialApp(
      home: Scaffold(
        body: Text('THIS IS A SECOND BODY'),
      ),
    );
  }
}
