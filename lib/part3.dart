import 'package:flutter/material.dart';
import 'main.dart';

class h extends StatelessWidget {
  
  Widget build(BuildContext context) {
    
    return MaterialApp(
      home: Scaffold(
        body: Text('THIS IS A FIRST BODY'),
      ),
    );
  }
}
